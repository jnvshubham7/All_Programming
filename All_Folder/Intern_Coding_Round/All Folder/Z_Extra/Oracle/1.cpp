//https://leetcode.com/discuss/interview-question/2418631/Oracle-OA-or-Binary-Game-or-Aug-2022
// A binary string is a string consisting only of 0 and 1. 
// A binary string is said to be good if:

// The digit 1 only appears in groups of size one_group, 
// if it apperars at all. For example, for one_group = 2, 
// "011110110" satisfies this condition while "01101010" does not.
// The digit 0 only appears in groups of size zero_group, 
// if it appears at all. For example, for zero_group = 3, 
// "11" satisfies this condition while "101010" does not.
// For example, for one_group = 2, zero_group = 1, the strings 
// "011", "000" are good while the strings "001" and "111" are not.

// You are given four integers min_length, max_length, zero_group 
// and one_group. Find the number of good binary strings of length
//  between min_length and max_length . The count may be large, so 
//  return the count modulo (10^9 + 7).

   #include<bits/stdc++.h>
   using namespace std;
   #define ll long long
   #define vi vector<int>
   #define pii pair<int,int>
   #define fr(i,n) for(int i=0;i<n;i++)
   #define all(v) v.begin(),v.end()
   #define mem(a,b) memset(a,b,sizeof(a))
   #define MOD 1000000007
   #define pb push_back
   #define mp make_pair
   #define pii pair<int,int>
   #define vll vector<ll>
   #define vc vector<char>
   #define vs vector<string>
   #define ppi pair<int,pair<int,int>>
   #define ppd pair<double,pair<int,int>>
   
   int main(){


   
   
    return 0;
   }
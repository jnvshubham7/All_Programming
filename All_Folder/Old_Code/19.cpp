#include <iostream>
using namespace std;
int main()
{
    freopen("input.txt", "r", stdin);
freopen("output.txt", "w", stdout);
    int a[i], a[j];
    if {
        a[i] > a[j];
        
        cout<<1<<" ";
        
    }
    if else {
        a[i] < a[j];
        
        cout<<1<<" ";
        
    }
    else {
        a[i] = a[j];
        
        cout<<" ";
}
return 0;
}


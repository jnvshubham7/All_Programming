class Solution{   
  public:
    vector<int> farNumber(int n,vector<int> a){
      // int max_diff = 0;
      // int max_index = 0;
      // int min_index = 0;

      // for(int i = 0; i < n; i++){
      //   if(a[i] > a[max_index]){
      //     max_index = i;
      //   }
      //   if(a[i] < a[min_index]){
      //     min_index = i;
      //   }

      //   if(abs(a[max_index] - a[min_index]) > max_diff){
      //     max_diff = abs(a[max_index] - a[min_index]);
      //   }


      // }
      // vector<int> result;
      // result.push_back(max_index);
      // result.push_back(min_index);
      // result.push_back(max_diff);
      // return result;



        
        
        
        // vector<int> v;
        
        // for(int i=0;i<n;i++)
        // {


            
        // }


        //code here
    }
};
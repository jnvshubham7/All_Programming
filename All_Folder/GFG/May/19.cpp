class Solution
{
    public:
    int transform (string A, string B)
    {

        // Given two strings A and B. Find the minimum number of steps required to transform string A into string B. 
        // The only allowed operation for the transformation is selecting a character from string A and inserting 
        // it in the beginning of string A.
        int na=A.size();

        int nb=B.size();

        

      




        for(int i=0;i<na;i++)
        {
            for(int i=0;i<nb;i++)
            {

                


            }
            
        }












        
        //code here.
    }
};

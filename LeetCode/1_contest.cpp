#include<bits/stdc++.h>
using namespace std;
#define ll long long

int main(){

int t;
cin>>t;
while(t--){

    int n;
    cin>>n;
    int a[n];
    for(int i=0;i<n;i++)
    {
        cin>>a[i];
    }




    //take any 2 consecutive elements of  and replace them with a single element equal to bitwise  of those two numbers.
   // Find whether Karan can make all the elements of the remaining array equal 
   //such that the size of the array is greater than or equal to 2 after performing any number of operations.

   
//take 2 consecutive elements of  and replace them with a single element equal to bitwise  of those two numbers.
//Find whether Karan can make all the elements of the remaining array equal




}

    return 0;
}